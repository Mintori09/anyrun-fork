[package]
name    = "symbols"
version = "25.12.0"
edition = "2021"

[lib]
crate-type = [ "cdylib" ]

[dependencies]
anyrun-helper = { workspace = true }
abi_stable    = "0.11.1"
anyrun-plugin = { path = "../../anyrun-plugin" }
ron           = "0.8.0"
serde         = { features = [ "derive" ], version = "1.0.228" }
